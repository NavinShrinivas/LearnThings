// variables2.rs
// Make me compile! Execute the command `rustlings hint variables2` if you want a hint :)

// I AM DONE

fn main() {
    let x : i32; //If not initialised with value, type must be given
    x = 10; //Comparing without init
    if x == 10 {
        println!("Ten!");
    } else {
        println!("Not ten!");
    }
}
